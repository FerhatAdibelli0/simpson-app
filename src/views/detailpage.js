import React, { Fragment } from "react";
import Detail from "../components/detail";

const Detailpage = () => {
  return (
    <Fragment>
      <Detail />
    </Fragment>
  );
};

export default Detailpage;
